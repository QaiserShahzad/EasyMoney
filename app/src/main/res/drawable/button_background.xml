<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_pressed="true" >
        <shape android:shape="rectangle" >

            <!-- View background color -->
            <solid
                android:color="@color/colorPD8" >
            </solid>

            <stroke
                android:width="1dp"
                android:color="@color/colorPD6" />

            <!-- The radius makes the corners rounded -->
            <corners
                android:radius="17dp">
            </corners>
        </shape>
    </item>
    <item>
        <shape android:shape="rectangle" >

            <!-- View background color -->
            <solid
                android:color="@color/white_five" >
            </solid>

            <stroke
                android:width="1dp"
                android:color="@color/white_five" />

            <!-- The radius makes the corners rounded -->
            <corners
                android:radius="17dp">
            </corners>
        </shape>
    </item>
</selector>